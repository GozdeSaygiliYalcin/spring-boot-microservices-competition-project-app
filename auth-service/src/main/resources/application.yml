server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/competitionDB
    username: postgres
    password: 2211223
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true