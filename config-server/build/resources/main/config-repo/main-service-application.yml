server:
  port: ${SERVERPORT}

spring:
  datasource:
    url: ${DBURL}
    username: ${DBUSERNAME}
    password: ${DBPASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

raceapplication:
  url:
    auth: ${AUTHURL}
    main: ${MAINURL}
    user: ${USERURL}