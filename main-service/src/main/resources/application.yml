server:
  port: 9092

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/competitionMainDB
    username: postgres
    password: 2211223
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true